# IMAP settings
IMAP_HOST=imap.web.de
IMAP_PORT=993
IMAP_USER=your.name@web.de
IMAP_PASSWORD=your_app_password
IMAP_SSL=true
IMAP_FOLDERS=INBOX,Sent,Junk

# Archive + DB
ARCHIVE_ROOT=/data
STATE_DB=/data/index/mail.db

# Optional
LOG_PATH=/data/logs/mail-archiver.log
