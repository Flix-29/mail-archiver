services:
  mail-archiver:
    build:
      context: .
    env_file:
      - ./config/.env
    volumes:
      - /Volumes/mail-archive:/data
      - ./state:/state
      - ./config:/app/config:ro
    restart: "no"
